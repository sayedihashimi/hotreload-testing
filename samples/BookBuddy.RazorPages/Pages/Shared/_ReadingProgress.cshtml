@model BookBuddy.RazorPages.Models.ReadingGoal

<div class="card mt-4">
    <div class="card-header">
        <h3>@Model.Year Reading Goal</h3>
    </div>
    <div class="card-body">
        <p>@Model.Description</p>
        <p class="text-muted">
            <small>Track your progress throughout the year!</small>
        </p>
    </div>
</div>
